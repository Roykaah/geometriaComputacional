
#include <stdio.h>
#include <stdlib.h>
#include <GL/glew.h>
#include <GL/freeglut.h>
#include "dcelStructures.h"

void connect(po)

int main(int argc, char **argv)
{
    //Criando o polígono do exercício
    vetor a,b,c,d,e,f,g;
    f.x = 0; f.y = 0;
    e.x = 0; e.y = 1;
    d.x = 1; d.y = 2;
    c.x = 2; c.y = 2;
    b.x = 2; b.y = 1;
    a.x = 1; a.y = 1;
    g.x = 1.2; g.y = 0.8;

    connect (a,b);
    
    
    
}








